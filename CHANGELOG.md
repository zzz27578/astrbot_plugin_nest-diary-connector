# 更新日志

## 0.6.0 - 2026-09-25

本次更新对照 AstrBot v4.28.1 做了一次整体体检：修正未来任务的权限与时区、收紧私密备忘录、调整回忆顺序，修复数据路径安全问题，并清理不再使用的旧代码。现有功能保持不变，小窝更轻、更快、更稳。

### 未来任务

- 权限与 AstrBot 内置未来任务走同一套逻辑：只有小窝从 WebUI 日记本设置挂载的托管任务（带 `managed_by` 标记）才跳过权限检查；用户通过 AstrBot 未来任务自建的任务按创建者身份（`sender_id`）执行小窝权限。普通群员不能再借定时任务读取、删除备忘录或改写人物印象。
- 托管任务按插件配置的“小窝时区”挂载，不再跟随服务器时区。
- WebUI 的写日记规范和人物印象规范随任务说明一起下发。AstrBot 的定时唤醒不经过 `on_llm_request`，此前这些规范没有送到定时写日记的 bot 手里。
- 插件停用、卸载或重载时撤下自己托管的任务，重新启用后自动挂回；重启 AstrBot 时保留已挂载的任务和运行记录。同步时不会接管或删除用户自建的同名任务。
- 同步失败会记录日志；读取日记本失败时不再误删已有任务。

### 旧数据兼容

- 升级无需迁移，旧的日记、日记本、修订快照、备忘录、人物印象、媒体、设置和备份照常使用。
- 旧版本写下的非补零日期（如 `2026-9-5`）继续指向原文件；日期名不规范的旧日记和旧媒体仍会列出，并且可以读取、搜索和删除。
- 设置文件里如果有旧版本留下的未知字段，会被忽略，不再导致整份设置读取失败。
- 插件配置文件名、数据目录和工具名都没有变化，原有配置不会丢失。

### 私密备忘录

- 私密备忘录只有小窝管理员可以写入、读取和删除。未配置小窝管理员时以 AstrBot 管理员为准，不再退化为“所有人都是管理员”。
- 非管理员搜索备忘录时看不到私密条目；管理员搜索时私密正文仍然遮住，需要用 `read_memo` 读取。
- 工具说明和隐藏规范写明：用户的私密信息建议存为私密备忘录。

### 回忆顺序

- 回忆具体信息（账号提示、约定、偏好、名言、待办）时，先查备忘录，没有再搜索或读取日记。工具说明、隐藏规范和 `nest-diary` skill 同步调整。

### 安全修复

- 日记与媒体的日期只允许数字和连字符，修复带 `../` 的日期能把文件写到日记本目录外、或读取目录外 `.md` 文件的问题；新写入的日期还必须是真实存在的 `YYYY-MM-DD`。
- 链接安装模块时拒绝绝对路径和盘符路径；解压前先校验全部文件，并限制解压后总大小为 64MB，校验通过前不会删除旧模块。
- 图片推送模板改用 Jinja 沙箱渲染，日记正文里的 `{{ }}` 不再被远程渲染器二次解析。
- 修复 `send_media` 因缺少参数说明被注册成无参数工具、每次调用都失败的问题；`write_diary` 补上 `notebook_id` 参数说明，定时任务能正确指定日记本。

### 轻量化与性能

- 删除从未启用的旧定时轮询循环、未被调用的 dataclass 工具、独立服务模式客户端和其他死代码，`main.py` 从 3748 行减到 2771 行。
- 插件配置只读取已声明的核心项，监听地址默认值统一为 `127.0.0.1`；`小窝绑定提醒` 改为引导绑定日记本。
- WebUI 设置读取加入按文件变化失效的缓存，一次工具调用不再反复读取和解析 `service-ui.json`。
- 设置、日记本、日记、人物印象、备忘录和媒体清单改为原子写入，bot 与 WebUI 同时读写时不会读到写了一半的文件。
- 图片裁剪和 QQ 兼容转码移出事件循环，推送大图时不再卡住其他消息。
- 隐藏规范的触发词收窄（去掉“今天”“记录”“图片”等日常词），日常聊天不再每次附带一大段规范。
- 重载插件时等待内置 WebUI 退出并释放端口，同时重新加载后端代码，不必重启 AstrBot。
- 移除已弃用的 `@register` 装饰器，消除 AstrBot 4.28 的弃用警告。

### 兼容性

- `astrbot_version` 声明为 `>=4.24.2,<5`：插件页内置 UI 从 4.24.2 开始提供；插件页里的导入导出、头像上传和媒体下载需要 4.26 以上。所用接口已对照 AstrBot v4.28.1 源码核对。

### 文档

- README 补充外部端口直连的登录安全说明：开放 `0.0.0.0` 前请先在 WebUI 生成访问密钥并修改默认密码；媒体原图地址不校验登录，仅建议在可信内网开放。

### 验证

- 新增未来任务身份、托管任务时区与清理、私密备忘录、日期与模块包路径安全、旧数据兼容、设置缓存等 15 项测试。
- 升级演练：用 0.5.23 写入日记（含非补零日期和修订快照）、多个日记本、备忘录（含私密）、人物印象、媒体和自定义设置，再用 0.6.0 读取。列表、全文、搜索、设置和 WebUI 接口与旧版完全一致，没有任何旧文件被改动或删除（只重建了搜索索引）。
- 回退演练：0.6.0 写入的数据用 0.5.23 读取，结果一致。
- 备份演练：0.5.23 导出的完整备份导入全新的 0.6.0，全部内容一致还原。
- 完整测试通过。

## 0.5.23 - 2026-09-02

本次更新修复日记本自动写入对应的 AstrBot 原生未来任务重复注册问题。

### 修复

- 修正未来任务查询过滤条件，避免每次保存设置都误判任务不存在并重复创建。
- 同步时保留并清理同名的全部历史任务，最终每个日记本只保留一个任务。
- 删除未来任务时优先使用 AstrBot 的公开 `job_id`，避免使用数据库自增 ID 导致删除失效。
- 自动恢复被意外禁用或配置已变化的插件任务。

### 验证

- 新增未来任务重复同步、历史重复任务清理、公开 job_id 删除和禁用任务恢复测试。
- Python、JavaScript 语法检查通过，完整测试通过。


## 0.5.22 - 2026-08-07

本次更新完整修复自定义模块在 UI 中点击卸载按钮无响应，以及保留数据卸载后模块仍可能残留的问题。

### 修复

- 在全局点击事件委托选择器中补齐 `data-module-uninstall`，让卸载按钮能正确进入卸载处理流程。
- 新增卸载进行中状态、重复点击防护、按钮禁用与失败恢复，并在成功后同步刷新模块列表、侧边栏和详情页。
- 修正“保留数据”语义：卸载时仅保留 `modules/<id>/data`，删除模块声明、页面与其他运行文件，且不再将仅剩数据的目录识别为已安装模块。
- 修复后端模块与前端扩展分离存储时的备份路径冲突，并改为完成全部备份后再执行删除。
- 补齐 `skins/<id>`、`hidden_module_nav_ids` 和当前外观配置的卸载清理，并使备份时间戳精确到微秒以避免碰撞。

### 验证

- 新增保留数据卸载、分离式模块完全卸载、备份目录隔离、配置清理与官方模块保护测试。
- 新增前端事件委托与卸载调用回归断言。

## 0.5.21 - 2026-08-05

本次更新修复自定义模块在 AstrBot 内置 UI 中无法加载的问题，并补齐模块创建后的重启提示和常见问题说明。

### 修复

- 修复内置 UI 直接动态导入 `/api/ui/module-assets/<module-id>/page.js` 时，请求误入 AstrBot Dashboard 并返回 401，最终显示 `Failed to fetch dynamically imported module` 的问题。
- 内置插件页现在通过 bridge 获取模块入口脚本，再从浏览器内存中导入；独立 WebUI 继续使用原生动态导入。
- 外观设置页右下角的“保存外观”按钮改为单列右对齐，并补充一致的上下及右侧留白。

### 变更

- `nest-module-development` skill 明确要求 bot 在直接创建或修改模块文件后，告知用户重启 AstrBot（或重载插件）再刷新页面。
- 模块安装弹窗新增“内置 skill 与常见问题”，说明侧边栏入口声明、启用状态、入口隐藏和校验失败的排查顺序。
- README 与模块化文档同步补充模块开发 skill、重启要求和常见问题。

### 验证

- 内置插件页与独立 WebUI 的 JavaScript/CSS 副本保持一致。
- JavaScript 与 Python 文件通过语法检查；未执行需要外部依赖或运行环境的拓展检查。

## 0.5.20 - 2026-08-04

自定义模块从"能装但看不见"变成真正可用：模块可以自己声明侧边栏入口和页面，小窝按声明渲染。

### 新增

- 模块能力声明：`module.json` 新增 `runtime`、`nav`、`page`、`store` 四个字段，模块声明自己参与哪些能力面。没有声明的能力面不会开放，只提供工具的模块不会顶着空入口和空页面。
- 轻量模块档位：`runtime: "webui"` 的模块只需要 `module.json` 和 `page.js`，不必自带后端，bot 可以独立产出一个可运行的模块。
- 模块页面加载：侧边栏改为由模块清单生成，新增 `/m/<module-id>` 路由和动态面板，SPA 按声明 import 模块自带的 `page.js` 并调用 `mount(root, ctx)`。
- 页面能力包：`ctx` 提供存储读写、命名空间接口调用、资源地址、通知、报错、确认弹窗、图标和刷新，模块不需要自己处理登录态与插件页桥接。
- 模块隔离存储：新增 `/api/ui/modules/<module-id>/store`，数据落在 `modules/<module-id>/data/store/`，跟着导入导出和备份走。单文档 1 MB、单模块 64 键。
- 模块资源路由：新增 `/api/ui/module-assets/<module-id>/`，收在 `/api/ui/` 前缀下，第三方模块在 AstrBot 插件页内置 UI 里无需改桥接白名单即可使用。
- 模块卸载：自定义模块、拓展包和外观包可以卸载，支持"保留数据"，删除前一律备份到 `imports/module-uninstall-backups/`。官方模块仍不可卸载。
- 侧边栏入口开关：已启用的模块可以单独隐藏侧边栏入口，模块本身继续可用。
- 模块详情页展示能力清单、声明校验结果、入口开关和卸载入口。
- 新增 `skills/nest-module-development/SKILL.md`，给 bot 和开发者一条稳定的模块开发链路。
- 新增示例模块 `examples/habit-board/`，展示轻量档的完整写法。

### 变更

- 官方模块 ID、官方外观 ID、保留目录名和内置图标名收进 `nest_diary_web/module_registry.py` 单一注册表，不再散落在三处硬编码。
- 声明 `runtime: "python"` 的模块包不会在安装后自动启用，必须由用户手动开启，安装结果会说明原因。
- 文档不再把独立端口当作使用前提，改为声明支持内置 UI；插件配置里的监听地址和端口标记为进阶项，默认监听地址收紧为 `127.0.0.1`。
- `skills/nest-webui-customization/SKILL.md` 纠正为只负责外观与框架层，并移除它此前描述的、SPA 实际不会加载的模块 `templates/` 路径。

### 修复

- 模块入口的三层把关补齐框架验真环节：声明的页面文件或图标不存在时收回入口，并在模块卡片上写明原因，而不是静默失效。
- 模块页面加载失败只降级该模块，其余部分不受影响，并给出可排查的报错。
- 前端目录里的模块声明会重新参与能力解析，不再被数据目录的旧声明覆盖。

## 0.5.19 - 2026-07-20

### 修复

- 修复 AstrBot 插件页运行时生成的 SVG 图标地址没有携带 Page `asset_token`，导致侧栏和模块图标全部显示为损坏图片；图标地址现在以 `import.meta.url` 为基准并继承鉴权参数。
- 插件页自定义头像改由 bridge 读取为 Data URL，不再直接请求需要独立 WebUI Cookie 的 `/api/ui/avatar`。
- 停止根据日记 `people` 名称自动创建无实际内容的候选人物档案，并在启动时清理旧版遗留的纯占位候选档案。
- “按 QQ 合成总体印象”现在优先命中原有 QQ 档案，自动移除同昵称占位档案，并用新证据完整重写原档 `summary`，不再追加 `【昵称】` 分段或新建同一人物。
- 统一/挂载身份策略下，新昵称建档必须提供 `qq_id`；人物编辑页新增 QQ 标识输入框，旧版默认印象提示词会自动升级为新的身份合并与总体重写规范。

## 0.5.18 - 2026-07-20

### 修复

- 修复 AstrBot bridge 自动解包顶层 `data` 后，插件页丢失代理响应的 `ok` 和 WebUI 地址元数据，导致完整界面只显示 `WebUI request failed (unknown)` 的问题。
- WebUI 代理与文件上传接口增加独立 bridge envelope，前端同时兼容已封装、已解包和旧版直接数据三种响应形态。

## 0.5.17 - 2026-07-20

### 修复

- 插件页不再显示状态中转卡片，改为直接加载与独立端口一致的完整小窝 WebUI。
- 插件页通过 AstrBot 页面桥接转发 WebUI 数据请求，已登录 Dashboard 后无需再次输入小窝管理员密码。
- 修复受限 iframe 内无法使用浏览器原生确认框、弹出原图和直接下载备份的问题，改为内置确认弹窗与 AstrBot bridge 上传下载。

### 安全

- 独立 WebUI 端口继续保留管理员密码验证；免密能力仅限已经通过 AstrBot Dashboard 身份验证的插件页面请求，不会把公网 `28080` 端口改成无保护访问。

## 0.5.16 - 2026-07-20

### 修复

- 修复 AstrBot 插件入口页未等待页面桥接初始化，导致状态卡片长期停留在“读取中”并显示 `Failed to fetch` 的问题。
- 插件入口页改为通过官方页面桥接读取状态，并兼容新版 `astrbot.api.web.json_response` 与旧版 Quart 响应方式。

### 调整

- 重做插件入口页的状态反馈、错误恢复、移动端布局和深色模式；读取失败时可重新尝试，并保留默认 WebUI 地址作为备用入口。

## 0.5.15 - 2026-07-07

### 修复

- 修复人物印象自动触达会把每次日记关联人物都追加成“自动补充证据”备注，导致读取档案时出现大段重复证据日期和备注的问题。
- bot 写入人物印象时改为合并式更新：保留旧档案的有效结构化信息，清理旧自动证据噪声，并把新证据作为真正的人物档案更新落盘。

## 0.5.14 - 2026-05-24

### 新增

- WebUI 首次进入新增遮罩式引导，会暗化全局、定位高亮目标并用小提示说明外观、模块、日记、媒体和备忘录入口；完成后写入设置，不会每次弹出。
- 新增官方备忘录模块，可由人或 bot 写入纸条，支持标题、正文、标签、来源聊天、记录者、置顶、归档、删除和敏感内容默认隐藏。
- 模块控制台新增“从链接安装”弹窗，支持 GitHub 仓库和 zip 包；覆盖同名模块前自动备份。
- 新增三套官方全局外观：纸庭、玻璃小屋、夜间工作室，可在外观设置中切换。
- 导入导出新增备忘录范围；完整备份会带上框架与模块数据摘要，导入前会预检备份包和当前小窝的最近日记日期。

### 调整

- 设置页重新分层为外观设置、模块控制台、访问密钥、导入导出，减少无关选项堆叠。
- 小窝 WebUI 的模块设置入口统一路由到“外观设置”；纸庭、玻璃小屋、夜间工作室三套官方全局外观收束到外观设置，不再作为普通模块卡片散落展示。
- 模块控制台新增备忘录设置，可控制模块开关、bot 自主写入策略、12 小时写入上限、敏感内容默认隐藏和非管理员备忘录权限。
- 删除旧的官方工业/战术全局外观模块，外观选择改为更适合长期阅读和私有管理的风格。
- 下拉、文件选择、弹窗、按钮、外观卡片统一为小窝自有视觉语言，减少系统默认控件痕迹。
- 保存外观时自动同步启用的全局外观模块，避免样式选择和模块开关状态不一致。

### 修复

- 重构夜间工作室为真正的全局深色模式，覆盖首页、媒体标题和工具栏、设置页、模块卡片、按钮、表单、备忘录和引导层，减少夜间泛白区域。
- 玻璃小屋和纸庭补齐媒体标题、备忘录纸条板和新引导层的外观覆盖。
- 修复设置保存时首次引导完成状态可能丢失的问题。
- 收紧全局弹窗点击判定，避免在弹窗内部操作时被误判为点击遮罩。
- 修复日记导出未包含新版多日记本正文目录的问题，分层导出现在会带上 `modules/diary/notebooks` 与日记本 registry。
- 导入完成后显示导入前/后的日记数量、最近日记日期、备忘录数量、媒体数量和导入前快照路径；如果健康检查发现最近日记倒退或数量减少，会显示风险提示。

## 0.5.13 - 2026-05-21

### 新增

- 日记图片推送新增“图片失败重试”和“图片失败提示”设置，默认失败后重试 3 次。
- 图片失败重试次数在后端强制限制为 0-10 次，防止配置异常导致反复刷屏或阻塞。

### 调整

- QQ/NT 富媒体发送失败时会按设置次数继续重试；达到上限后，可按开关在目标会话发送简短失败提示。

## 0.5.12 - 2026-05-21

### 修复

- 修复 QQ/NT 端偶发 `rich media transfer failed` 时图片推送直接失败的问题；日记图片发送失败后会自动转为更稳的 RGB/JPEG 并通过 base64 重发。
- 压缩重发会限制图片宽度和总像素，保留可读性，同时降低 NT 富媒体上传失败概率。
- 图片发送失败时返回更明确的原因，区分渲染问题和 QQ/NT 富媒体传输问题。

## 0.5.11 - 2026-05-21

### 修复

- 修复 AstrBot T2I 端点返回超大白色画布时，日记卡片只出现在左上角的问题；渲染成功后会按有效内容自动裁掉右侧和底部空白。
- 日记图片裁切对 `push_diary` 工具和同一后端推送链路统一生效，旧日记本读取后的图片推送也会使用新版裁切策略。
- 显式补充 Pillow 依赖，用于本地识别并裁切 T2I 返回图片。

## 0.5.10 - 2026-05-21

### 修复

- 重做日记图片推送渲染链路：先在本地生成完整 HTML，再交给 AstrBot T2I 端点按整页长图渲染，不再使用局部 selector 截图，避免有效内容缩在左上角。
- 新增 PNG 优先、JPEG 回退的两轮渲染策略，并校验 T2I 返回结果必须是有效 PNG/JPEG，防止网络端点错误页被当作图片发送。
- 当自定义 HTML 渲染端点不可用时，自动回退到 AstrBot 普通文本转图能力，避免推送直接中断。
- 图片发送端兼容本地文件、`file:///`、网络 URL、`base64://`、data URI 和 bytes，避免渲染结果格式与 AstrBot 图片组件不匹配。
- 保持日记推送目标按当前真实会话协议解析，不再回退到写死的私聊类型。

## 0.5.9 - 2026-05-20

### 修复

- 修复旧日记本在 AstrBot 会话协议从 `private` 变化为 `FriendMessage` 等具体类型后，被误判为新会话并自动新建日记本的问题。
- 新增日记本协议轻量审查：启动时会给已对齐的旧日记本补齐协议识别标签；真实会话进入时会按平台、会话 ID 和私聊/群聊语义迁移旧 UMO，并保留旧来源别名。
- 修复管理员私聊推送目标硬编码为 `平台:private:QQ号` 的问题；现在会优先使用当前真实私聊来源、完整管理员 UMO、已绑定日记本里的真实消息类型。
- 修复管理员私聊读取日记时只识别字面 `private`，导致 `FriendMessage` 场景无法安全检查其他日记本的问题。
- 修复日记推送时旧日记文件中的过期 `origin_umo` 覆盖日记本绑定来源，导致 `source` 推送可能发往旧协议目标的问题。
- 图片推送模板改为固定宽度、内容自适应高度的长图结构，并使用 `.diary-push-page` 精准截图，避免生成大画布但有效内容只挤在左上角。

## 0.5.8 - 2026-05-20

### 修复

- 修复隐藏运行规范注入时，旧设置对象缺少 `diary_write_prompt` 等新字段导致对话流程报错的问题。
- 修复类工具执行 `push_diary` 时没有正确从 AstrBot `ContextWrapper` 中取出真实会话事件，导致日记本 ID 被解析成 `session___...` 的问题。
- 修复日记推送找不到对应日期时直接暴露服务器文件路径的问题；现在会返回普通用户能理解的提示，并会兼容检查默认日记本。
- 管理员私聊推送时，若当前日记本没有该日期日记，会安全检查其他日记本；若多个日记本命中则停止并提示选择，避免串群推送。

## 0.5.7 - 2026-05-20

### 修复

- 彻底修复 WebUI 日记模块配置里日记本删除不持久的问题：删除标记会随表单一起提交，后端按当前日记本完整列表覆盖保存，避免旧缓存或漏传删除名单导致日记本复活。
- 设置接口现在同步返回最新日记本列表，模块配置重新打开时不会继续使用旧列表。
- 日记本删除会同步移除对应日记本目录，确保注册表和文件数据一致。

## 0.5.6 - 2026-05-20

### 调整

- 媒体页切换到其他页面时会先关闭漂浮布局并记住用户偏好；回到媒体页后先按普通网格定位，再恢复漂浮，避免图片从左上角挤成一团。
- 图片推送模板弹窗加宽，预览区放大；自定义模板改为可保存多套，并以卡片形式和官方模板一起选择。
- AstrBot 插件配置继续精简，只保留 WebUI 启动、端口、密码、管理员 QQ、数据目录和时区；具体功能统一进入 WebUI 设置。
- 元数据作者改为“百岁老太”，支持平台收敛为 QQ 场景的 `aiocqhttp`。
- README 按说明书重写，补充快速开始、模块生态、工具清单、隐藏规范、日记本隔离和媒体模块说明。

### 修复

- 修复部分已保存日记本删除后没有进入后端删除名单，保存并关闭后又重新出现的问题。
- 设置接口现在返回最新日记本列表，避免设置页反复使用旧的 bootstrap 日记本缓存。

## 0.5.5 - 2026-05-20

### 调整

- 日记本管理改为正式管理界面：新增按钮会生成新日记本行，现有日记本提供删除按钮；默认日记本保留为基础入口，不能误删。
- 日记本绑定会话改成普通用户能看懂的“私聊/群聊 + QQ号或群号”，不再暴露平台 UMO 格式。
- 图片推送模板从代码文本框改为模板选择弹窗，内置“清简便签”“终端报告”“杂志页”三套官方模板，并保留自定义模板入口。
- “写日记提示词”改名为“写日记要求规范”，后台定时写日记和普通小窝工具调用都会把它作为隐藏规范注入给 bot。
- AstrBot 插件配置精简为启动、端口、登录、定时器等底层项；模块开关、外观、媒体、人物印象等具体功能设置回到 WebUI。

### 修复

- 修复媒体漂浮模式在切换页面时因为隐藏面板尺寸为 0，把图片坐标写成左上角后再炸开的布局问题。
- 媒体页离开后会清掉漂浮坐标，回到媒体页时先按普通网格定位，再进入漂浮，避免切页后挤到左上角。
- 修复日记本新增只有输入条、没有明确新增按钮和删除入口的问题。
- 修复日记模块开关只在插件启动时读取的问题；现在日记工具和定时任务会实时读取 WebUI 设置。
- 修复后台每日写日记提示词和人物印象提示词可能重复、越过 WebUI 开关生效的问题；人物印象规范现在只在 WebUI 开关允许时拼接。
- 定时写日记优先读取 WebUI 日记本绑定和写日记时间，插件配置里的目标会话和时间只作为旧配置兜底。

## 0.5.4 - 2026-05-20

### 调整

- 重排日记查看筛选区：日记本单独占一行，年月日收成紧凑分组，不再四个控件硬挤一排。
- 日记列表标题限制为最多两行，超出自动省略；列表区域和正文区域按比例放大，减少拥挤感。
- 日记模块设置改成“写日记规则、日记本管理、非管理员权限、推送样式”四块，移除容易误解的“按年/按月保存”和重复的全局推送开关。
- 日记本管理支持新增日记本，并可填写绑定群聊或私聊会话、显示名称、写日记时间和推送目标。
- 小窝管理员改成单个 QQ 输入；展示方式里的合并显示明确标注只影响页面展示，不影响群聊推送隔离。

### 新增

- 新增写日记提示词设置，会作为后台写日记规范注入给 bot。
- 新增图片推送模板设置，图片推送优先使用 AstrBot 文转图能力渲染。
- 新增 `push_diary` 工具，可由 bot 调用，把指定日期日记以文字或图片推送到原会话、管理员私聊或两边。
- 新增非管理员权限清单，默认无权限，可单独放开查看、搜索、写入、媒体、人物印象等权限。

## 0.5.3 - 2026-05-20

### 新增

- 日记改为按日记本隔离保存，同一个配置文件下的不同群聊、私聊可以拥有独立日记本；合并只作为展示方式，不再混写数据。
- 日记查找新增日记本/群组范围，搜索也可选择全部日记本或指定日记本，避免群组回忆串线。
- 设置页的日记模块新增小窝管理员、私聊写入、日记推送、推送格式、推送目标和日记本管理。
- 媒体模块新增写入限制策略、12 小时保存上限和相册整理策略，并接入真实后端字段。

### 调整

- AstrBot 工具会从 `event.unified_msg_origin` 解析当前会话日记本，群聊中的日记读写和搜索默认只作用于当前群组日记本。
- 小窝管理员可继续用自然语言操作；非管理员在群聊中不能通过工具写日记、改人物印象、保存或发送媒体。
- WebUI 设置中填写的小窝管理员会被插件工具读取，不再只依赖 AstrBot 插件配置。

### 修复

- 修复媒体“仅管理员保存”可能被旧的 bot 自动导入开关误拦的问题。
- 修复同一天不同日记本的日记在前端选择、编辑、删除时可能只按日期匹配的问题。
- 修复媒体策略前后端选项值不一致，导致保存后被重置为默认策略的问题。

## 0.5.2 - 2026-05-20

### 调整

- 小窝战术终端不再默认启用；官方外观模块只在模块管理中展示，需用户主动打开。
- 重做小窝战术终端配色，移除深色搭配亮黄色的强刺激方案，改成更干净的冷灰、蓝绿和少量警示红。
- 模块配置页去掉右上角小标签，放大标题比例，并调整返回、设置、保存按钮的尺寸和反馈。
- 导入导出页的导出范围改为多选卡片，导入策略和其他下拉控件改成更明确的短控件样式。

### 修复

- 导出接口支持一次选择多个导出范围，不再只按单个下拉值导出。

## 0.5.1 - 2026-05-20

### 调整

- 媒体页漂浮模式支持同时展开多个文件夹，展开后的文件夹固定在原地作为投放面板，不再参与其他图片的漂浮碰撞。
- 新建文件夹和回收站按钮统一为竖向工具组，回收站按钮与新建文件夹同宽，并在拖动靠近时显示更明显的红色投放范围。
- 新建文件夹按钮改用内置深色文件夹加号图形，避免浅色背景下官方图标看不清。

### 修复

- 修复漂浮模式下展开文件夹仍被碰撞层影响，导致图片不容易拖入文件夹的问题。
- 修复媒体拖拽释放到回收站边缘时判定不稳定的问题；现在会按鼠标点、卡片中心和卡片矩形与回收站热区相交综合判断。
- 修复未展开文件夹也可能接收图片投放的问题，只有已经展开的文件夹才会作为图片投放目标。

## 0.5.0 - 2026-05-20

### 调整

- 新增官方全局外观模块“小窝战术终端”，在模块管理的外观模块中显示和开关，默认首次启用。
- `/theme.css` 改为根据已启用外观模块加载样式；关闭官方外观模块后会回到底层默认界面。
- 模块冲突检测现在会把官方全局外观、自定义全局外观和旧主题样式一起纳入判断。
- 全站前端统一为更简洁有力的控制台风格：强化信息层级、短标签、硬朗卡片、按钮按下反馈、页面切换和弹窗动效。

### 修复

- 修复默认外观只存在于内置 CSS、无法通过模块管理真正启停的问题。

## 0.4.9 - 2026-05-20

### 调整

- 图片详情页备注默认只读，点击“修改备注”后才进入编辑状态，保存成功后会恢复只读并弹出右下角提示。
- 文件夹卡片不再直接显示编辑按钮；进入文件夹页面后，标题右侧提供“编辑文件夹”入口。
- 漂浮模式下展开文件夹时保持原地展开，投放提示移动到文件夹上方区域，原投放区改为“取消”收缩按钮。

### 修复

- 修复新建文件夹和保存备注读取不到当前表单数据，导致按钮看似可点但没有真实生效的问题。
- 修复漂浮文件夹展开动画覆盖漂浮坐标，导致文件夹跑到左上角的问题。
- 修复回收站彻底删除在媒体记录缺失或文件已不存在时卡住，无法清理回收站项目的问题。
- 修复图片详情弹窗内右下角提示被遮罩压住、看不到保存反馈的问题。

## 0.4.8 - 2026-05-20

### 调整

- 文件夹新增编辑入口，可修改名称、标签和备注 / 副标题，并写入真实媒体组织数据。
- 图片详情页的备注改为可编辑，保存后会更新媒体归档记录。
- 横屏图片详情页固定为图片在上、信息在下，并整体缩小展示，避免文字压到图片上。
- 图片详情页移除“图片尺寸”一栏，保留保存日期、文件大小和备注。

### 修复

- 修复新建文件夹弹窗中点击输入框会被误判为点击遮罩，导致弹窗关闭或页面切换的问题。
- 修复漂浮模式下文件夹展开后位置丢失、回到左上角的问题。
- 修复长比例图片查看时图片过大、文字区域和图片区域挤压的问题。

## 0.4.7 - 2026-05-20

### 调整

- 新建文件夹改为小窝内置弹窗，支持填写文件夹名称、标签和备注 / 副标题，不再使用浏览器默认提示框。
- 媒体页容量信息移动到“媒体”标题旁，同时显示回收站数量；右侧只保留漂浮模式、新建文件夹和回收站按钮。
- 漂浮模式下点击文件夹会先原地展开投放区，再次点击中心才进入文件夹页，避免文件夹跳到左上角。
- 关闭漂浮模式时增加回到网格排列的过渡动画，不再直接闪现。
- 图片详情弹窗改为全局弹层，按视窗比例缩小展示图片，尽量避免滚轮和被媒体区域裁切。

### 修复

- 修复漂浮模式下文件夹打开、回收站恢复和彻底删除按钮被拖拽逻辑拦截的问题。
- 修复放入回收站仍弹出浏览器确认框的问题；普通回收现在直接进入回收站，只有彻底删除保留确认。
- 修复文件夹标签和备注只停留在前端的问题，现在会写入媒体组织数据。

## 0.4.6 - 2026-05-20

### 调整

- 媒体漂浮区改为随页面和图片数量自然增长，不再被固定小框限制；页面可继续向下滚动，图片不会被挤在一块。
- 右上角媒体工具区保持可见，新建文件夹、回收站和漂浮模式开关在滚动整理时也能直接操作。
- 文件夹卡片支持展开投放区，图片放入文件夹后不再显示在主媒体页；文件夹页支持把图片拖出后移回外层。
- 回收站改为独立媒体子页面，回收项目提供恢复和彻底删除，彻底删除会同步移除媒体文件和归档记录。
- 漂浮模式速度调慢，图片可推动并保留惯性；拖到回收站或文件夹时会执行真实整理动作。

### 修复

- 修复拖拽释放被误判为点击，导致图片被意外打开的问题。
- 修复图片详情弹窗遮罩范围过小、双层滚动条和大图被顶部挤压的问题。
- 修复回收站投放区不在拖拽判定范围内，导致图片拖不进去的问题。

## 0.4.5 - 2026-05-19

### 调整

- 媒体详情弹窗改为视口居中展示，缩放浏览器后仍尽量保留完整图片。
- “打开原图”改为新窗口打开，不再关闭详情弹窗。
- 媒体页增加新建文件夹、回收站和拖拽整理。
- 图片拖入文件夹会保存归类关系，拖入回收站会软回收并可恢复。
- 增加漂浮模式开关，图片和文件夹会缓慢漂浮、碰撞，并可被推动产生惯性；关闭后自动回到网格排列。

# 0.4.4 - 2026-05-19

### 调整

- 媒体页改成缩略图图库，不再只显示文件名。
- 媒体详情支持按图片比例弹窗展示，并显示名称、日期、大小、备注。
- 媒体设置增加图片保存方式，可选复制或剪切。
- 媒体统计改为实时显示总占用空间。
- bot 侧新增原图发送工具，按保存文件直接发送，不压缩画质。
- 媒体归档工具增加隐藏备注字段，用于记录来源、情景和评价。

## 0.4.3 - 2026-05-19

### 调整

- 设置左侧改成可收纳形式，点击“设置”可以展开或收起“模块管理 / 访问密钥 / 导入导出”。
- 小窝 WebUI 继续保留官方模块身份，但在界面上归入外观模块一类展示。
- 首页副标题改为可配置，不再只允许修改主标题。
- 模块管理的启用开关改成真实保存，不再只是表面按钮。
- 模块卡片和侧边栏补上更轻的圆角、图标和过渡动画。
- 接入少量 Game Icon Pack 图标资源，用于导航、模块卡片和设置页装饰。

### 修复

- 关闭某个模块时，不再被隐藏输入字段误改回开启状态。
- 媒体模块仍保持独立控制，关闭后左侧入口隐藏，bot 导入和数量限制继续生效。

## 0.4.2 - 2026-05-19

### 调整

- 设置页继续收敛为普通用户可读的页面：右侧不再重复显示“设置”，模块管理、访问密钥、导入导出各自作为一级标题。
- 模块管理移除多余入口、无冲突提示和底部保存长条，模块详情页改为右下角“保存并关闭”。
- 保存成功改为右下角小提示，减少页面上不必要的说明文字。
- 模块卡片标签简化为中文词条，只保留官方模块、外观模块、补充拓展、全局替换等必要信息。
- 人物印象设置文案改为写入强度、更新策略、确认程度，并统一使用“人物”表述。
- 小窝 WebUI 明确作为官方功能模块展示，外观类只展示真正的外观模块。

### 修复

- 媒体模块从日记模块中独立出来，关闭媒体模块后左侧“媒体”入口会隐藏。
- 媒体模块关闭、bot 导入关闭或达到每日数量上限时，bot 侧媒体导入会真实拒绝，不再只是前端假开关。

## 0.4.1 - 2026-05-19

### 调整

- 设置入口改为左侧主侧边栏折叠收纳：点击“设置”后在其下展开模块管理、访问密钥、导入导出。
- 设置页右侧移除二级侧栏和小方块式入口，当前设置项直接展示为面向用户的管理页面。
- 模块管理改为插件管理式卡片网格；官方模块只作为卡片标签，不再单独划分为一整块。
- 外观模块作为独立筛选入口展示，主题、皮肤、全局替换和视觉拓展统一归入外观模块区域。
- 模块卡片新增图形化标识、运行状态、标签、配置按钮和启用/关闭按钮，替代长条嵌入式列表。
- 重新调整设置页按钮、文字间距、模块统计和冲突提示的视觉层级。

## 0.4.0 - 2026-05-19

### 新增

- 设置页改为模块管理台：侧边栏只保留模块管理、访问密钥、导入导出。
- 模块管理新增官方模块、自定义完整模块、补充拓展包、外观模块分组。
- 模块卡片新增启用开关、官方/自定义/拓展/外观标签、说明文本和设置入口。
- 单个模块设置改为独立详情页，并在左上角提供返回模块管理按钮。
- 新增外观模块分类，区分全局替换与补充拓展；多个全局替换同时启用时显示红色冲突提示。
- 人物印象模块新增日记后自动识别、写入程度、更新策略、是否允许新建候选档、最低置信度等设置。

### 修复

- 定时归档任务改为后台 Agent 工具循环执行，任务提示词不再作为可见消息发到聊天窗口。
- 归档后的可见提示只由“归档后是否提示”控制，不再泄漏执行提示词。
- 日记写入后不再因为 `people` 字段出现新称呼就自动建档人物印象。
- 写日记工具返回内容不再附带人物印象自检提示词。

### 文档

- 更新模块化目录、人物印象策略、外观模块、导入导出和隐藏归档提示词说明。
- 更新官方模块说明文件，明确人物印象与 WebUI 模块的职责边界。
